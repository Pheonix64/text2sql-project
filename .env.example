# ========================================
# Text-to-SQL Project - Environment Variables
# ========================================
# 
# Copiez ce fichier vers `.env` et ajustez les valeurs selon votre environnement
# 
# IMPORTANT: Ne commitez JAMAIS le fichier .env (contient des secrets)
# 

# ==================== API ====================
API_PORT=8008

# ==================== PostgreSQL ====================
# Base de données principale
POSTGRES_DB=economic_data
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgrespwd123!

# Hôte et port (pour connexions internes Docker)
DB_HOST=postgres-db
DB_PORT=5432

# Utilisateur LLM (lecture seule - pour l'API)
# Cet utilisateur est créé automatiquement par init.sql
LLM_USER=llm_user
LLM_PASSWORD=/-+3Vd9$!D@12

# ==================== ChromaDB ====================
# Hôte ChromaDB (nom du service Docker)
CHROMA_HOST=chroma-db

# Port INTERNE (utilisé par l'API pour se connecter à ChromaDB)
# NE PAS MODIFIER - doit rester 8000
CHROMA_PORT=8000

# Port EXTERNE (pour accéder à ChromaDB depuis l'hôte)
CHROMA_EXTERNAL_PORT=8088

# Nom de la collection vectorielle
CHROMA_COLLECTION=sql_reference_queries

# ==================== Ollama (LLM) ====================
# Hôte Ollama (nom du service Docker)
OLLAMA_HOST=ollama

# Port Ollama
OLLAMA_PORT=11434

# Modèle LLM à utiliser
# Options: mistral:7b, llama2:7b, llama2:13b, mixtral:8x7b, codellama:7b
LLM_MODEL=mistral:7b

# ==================== Embeddings ====================
# Modèle pour les embeddings de texte (recherche sémantique)
# Options populaires:
#   - sentence-transformers/all-MiniLM-L6-v2 (rapide, léger)
#   - sentence-transformers/all-mpnet-base-v2 (meilleur, plus lourd)
#   - sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2 (multilingue)
EMBEDDING_MODEL_NAME=sentence-transformers/all-MiniLM-L6-v2

# ==================== Optionnel ====================
# Niveau de log (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO

# Timeout des requêtes (en secondes)
# REQUEST_TIMEOUT=30
